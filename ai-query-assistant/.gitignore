# Local build artifact — auto-generated from the CSVs in data/ when the app first runs
olist.db

# Excluded: exceeds GitHub's 25MB browser-upload limit, and not needed by this app
data/olist_geolocation_dataset.csv

# Environment / secrets
.env
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit
.streamlit/secrets.toml
